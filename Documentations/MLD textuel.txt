FAMILIES = (FAMILIES_id COUNTER, name VARCHAR(50), owner_user_id INT, invite_code VARCHAR(50), created_at DATETIME, update_at DATETIME, is_active LOGICAL, avatar_url VARCHAR(255));
REWARD_TYPE = (REWARD_TYPE_id COUNTER, name VARCHAR(50), is_active LOGICAL, created_by INT);
CATEGORIES = (CATEGORIES_id COUNTER, families_id INT, name VARCHAR(50), color VARCHAR(50), created_by INT, created_at DATETIME, update_at DATETIME, is_active LOGICAL);
ROOM = (ROOM_id COUNTER, name VARCHAR(50), color VARCHAR(50), created_by INT, created_at DATETIME, update_at DATETIME, is_active LOGICAL, #FAMILIES_id*);
ROLE = (ROLE_id COUNTER, role VARCHAR(50), is_active LOGICAL, created_by INT, #FAMILIES_id*);
USERS = (User_ID COUNTER, email VARCHAR(255), password_hash VARCHAR(255), last_name VARCHAR(255), first_name VARCHAR(255), avatar_url TEXT, points INT, is_active LOGICAL, created_at DATETIME, update_at DATETIME, #ROLE_id);
TASKS = (TASKS_id COUNTER, title VARCHAR(50), description VARCHAR(255), status enum('en cour','validé','en attente','à faire'), priority enum('basse','haute','moyenne'), due_date DATE, reccurencce_type enum('jours','semaine','mois','années'), reccurence_value INT, task_points INT, categorie INT, created_at INT, update_at DATE, is_active LOGICAL, room INT, #FAMILIES_id*, #CATEGORIES_id, #User_ID);
VALIDATION_TASK = (VALIDATION_TASK_id COUNTER, validated_at DATE, validated_by INT, duration INT, is_active LOGICAL, families_id INT, #TASKS_id);
REWARDS = (REWARDS_id COUNTER, reward_point INT, reward_name VARCHAR(255), reward_duration INT, reward_created_by INT, reward_created_at DATE, reward_is_active LOGICAL, reward_update_at DATE, reward_description VARCHAR(255), #REWARD_TYPE_id, #FAMILIES_id);
users_families = (#FAMILIES_id, #User_ID, is_active LOGICAL);
task_room = (#ROOM_id, #TASKS_id, is_active LOGICAL);
